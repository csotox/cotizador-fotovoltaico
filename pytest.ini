[pytest]
DJANGO_SETTINGS_MODULE = app_config.settings
pythonpath = cotizacion
python_files = test_*.py *_test.py tests.py
addopts = -m "not e2e" -ra
markers =
    e2e: requiere un servidor en vivo (usa la base de datos real)